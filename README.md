# SConnect

Student, teacher, parent and admin school platform. Express + MongoDB backend,
React + Vite frontend, JWT auth with refresh rotation, Socket.io messaging.

See `SETUP.md` for install commands and the keys you need to create.
